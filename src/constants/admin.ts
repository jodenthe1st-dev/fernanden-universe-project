// Admin credentials should NOT be stored client-side.
// Use server-side environment variables `ADMIN_EMAIL` and `ADMIN_PASSWORD` and
// `ADMIN_TOKEN_SECRET` for production. This file kept for reference only.
export const ADMIN_CREDENTIALS = null;
